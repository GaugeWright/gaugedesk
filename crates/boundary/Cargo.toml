[package]
name = "gaugedesk-boundary"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
gaugedesk-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
proptest = "1"
