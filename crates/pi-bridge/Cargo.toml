[package]
name = "gaugedesk-pi-bridge"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
gaugedesk-env = { path = "../env" }
gaugedesk-core = { path = "../core" }
gaugedesk-harness = { path = "../harness" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
proptest = "1"
